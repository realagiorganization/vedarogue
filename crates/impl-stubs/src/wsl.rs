//! 4. WSL Setup – stubs corresponding to Makefile install-wsl and -in-docker

use crate::Result;

/// 4.1 Ensure WSL is installed on host
pub fn ensure_wsl() -> Result<()> { Ok(()) }

/// 4.2 Launch Linux dev container (WSL-like)
pub fn launch_wsl_like_container() -> Result<()> { Ok(()) }

