# Copy this file to .env and fill in values

# Human-friendly dataset name (optional)
DATASET_NAME=your_dataset_name

# Required: URL to download the dataset archive or file
# Example: https://example.com/data/archive.zip
DATASET_URL=

# Optional but recommended: SHA256 checksum to verify integrity
# Example: 0123456789abcdef... (64 hex chars)
DATASET_SHA256=

# Optional overrides
# DATA_DIR=data
# DATASET_FILENAME=custom_name.zip

# Hugging Face dataset options
# Default to the dataset you requested
HF_DATASET_ID=manojbalaji1/anveshana
# Optional: comma-separated list of splits to export (leave empty to export all)
# HF_SPLITS=train,validation,test
# Optional: specific revision (branch, tag, or commit SHA)
# HF_REVISION=
# Optional: auth token if dataset is gated/private; otherwise run `huggingface-cli login`
# HF_TOKEN=

# Docker toolchain images (required if not building locally)
# By default, Makefile expects locally built images via `make docker_build_all`:
# EMACS_IMAGE=vedarogue/emacs:latest
# TEX_IMAGE=vedarogue/tex:latest
# OCR_IMAGE=vedarogue/ocr:latest
# POPPLER_IMAGE=vedarogue/poppler:latest
# PY_IMAGE=python:3.11-slim
